"use client";

const sessionIdPage = () => {
  if (document === null) {
    return <div>Not found</div>;
  }

  return (
      <div className="max-w-full mt-10">
        Chat page
      </div>
  );
};

export default sessionIdPage;
