"use client";

const sessionIdPage = () => {
  if (document === null) {
    return <div>Not found</div>;
  }

  return (
      <div className="max-w-full">
        Chat page
      </div>
  );
};

export default sessionIdPage;
